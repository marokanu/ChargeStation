spring:
    datasource:
        url: jdbc:mysql://localhost:8080/bookstore
        username: root
        password: mercedes
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8082
